import { Section } from '@/components/section'

const Albums = () => {
  return (
    <Section title="Albums" hasLink href="/">
      <div>Albums</div>
      <div>Albums</div>
      <div>Albums</div>
    </Section>
  )
}

export default Albums
